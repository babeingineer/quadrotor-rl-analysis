# Trial 36 — xw36: 100 Hz policy rate (user request)

## Why
User directive after forum discussion of setpoint staleness (50 Hz policy = ~10 ms average
setpoint lag into the PID). Prior analysis predicts little effect on STEADY-STATE hold of a
constant target (staleness error scales with setpoint rate-of-change, ≈0 at hold; the
classical cascade hits 0.2 median through the same 50 Hz outer loop) — this run tests it.

## What (vs xw32: rate + matched-γ only)
`--ctrl-freq 100 --gamma 0.995` (γ rescaled to keep the ~2 s TIME horizon; episode stays
8 s = 800 steps; physics unchanged at 500 Hz, 5 substeps per action). Caveat: 8M steps at
100 Hz = half the simulated seconds of xw32's 8M — judge vs xw32 at matched WALL steps and
note the sim-time deficit.

## Pre-registered criteria (8 s rest protocol, robust n=300 eval; xw32b baseline 2.38 [2.28–2.56] @12M)
- **SUCCESS**: robust median CI fully below xw32b's → rate mattered; rerun ladder at 100 Hz.
- **NULL** (expected): CI overlaps xw32b's → staleness confirmed irrelevant for hold;
  documented and closed.
- **FAILURE**: CI above → halved sim-time/credit-assignment costs dominate; close.

## Result
*(auto-appended by watchdog)*

## VERDICT: NEGATIVE (with confounds acknowledged) — closed
Training collapsed mid-run: per-step eval reward 0.053 late vs 0.845 for the 50 Hz twin
(peak 298/800-step episode, then collapse). Confounds that bias 100 Hz down: gae_lambda
left at 0.95 (GAE time-window halved — my miss), n_steps spanning half the seconds, and
2× sample correlation making PPO updates effectively more aggressive. Even so, the
theoretical setpoint-staleness benefit (~10 ms) had no visible effect before the collapse,
matching the pre-registered NULL prediction and the classical-baseline evidence (0.2 m/s
median through the same 50 Hz outer loop). 50 Hz remains the standard. A fully fair retry
(λ 0.975, n_steps 4096) is possible but not queued — expected value too low vs the
wind-tail and high-band work.
